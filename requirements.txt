# Core dependencies
torch>=1.10,<2.1
pytorch-lightning>=2.0,<2.1
hydra-core>=1.3,<1.4
hydra_colorlog
wandb>=0.15,<0.16
rich>=13.0.0,<14.0.0
transformers>=4.27,<4.28
scikit-learn
datasets
black[d]

# Optional dependencies
# onnx GPU
optimum[onnxruntime-gpu]>=1.7,<1.8; platform_system == "Linux" # needed by: onnx-gpu
termcolor # needed by: onnx-gpu
# onnx CPU
optimum[onnxruntime]>=1.7,<1.8 # needed by: onnx
termcolor # needed by: onnx
# serve
fastapi==0.95.0 # needed by: serve
uvicorn[standard]==0.21.1 # needed by: serve
gunicorn==20.1.0 # needed by: serve
ray[serve]>=2.4,<=2.5 # needed by: serve
ipa-core # needed by: serve
